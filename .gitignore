# dependencies
node_modules/

# local build cache
/.vite/

# Keep the static deployment bundle available for hosts that deploy dist/ directly.
# Source deployments can still rebuild it with `npm run build`.

# TypeScript build cache
*.tsbuildinfo

# local environment files
.env
.env.*
!.env.example

# logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# editor and OS files
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.DS_Store

# local test and coverage output
coverage/
