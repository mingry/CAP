
# 비행기 슈팅 게임

## 목표

- 브라우저에서 바로 실행되는 프론트엔드 전용 3D 횡스크롤 슈팅 게임을 만든다.
- 별도 백엔드, 로그인, 데이터베이스, 서버 저장 기능은 추가하지 않는다.
- 처음 실행했을 때 사용자가 설명을 읽지 않아도 조작과 목표를 이해할 수 있어야 한다.

## 기술 기본값

- 기본 스택은 Vite + TypeScript + Three.js로 한다. 이미 프로젝트에 정해진 스택이 있으면 기존 스택을 우선한다.
- 외부 3D 모델과 무거운 에셋은 사용하지 않는다. 비행기, 적, 탄환, 보스는 Three.js의 기본 기하학 도형 조합으로 만든다.
- 게임 로직과 렌더링을 한 파일에 모두 몰아넣지 말고, 최소한 `game`, `entities`, `scene` 정도의 역할을 구분한다.
- 프레임마다 불필요한 객체를 새로 만들지 않는다. 가능한 객체는 재사용하고, 종료된 적과 탄환은 정리한다.
- 새 라이브러리는 꼭 필요한 경우에만 추가한다. 기능보다 단순성과 안정성을 우선한다.

## 게임 플레이

- 화면은 플레이어가 왼쪽 또는 중앙에 있고, 적과 배경이 오른쪽에서 왼쪽으로 이동하는 횡스크롤 구조로 한다.
- 플레이어는 키보드 방향키로 상하좌우 이동하고, 스페이스 바로 총알을 발사한다.
- 플레이어가 화면 밖으로 완전히 나가지 않도록 이동 범위를 제한한다.
- 게임 시작 후 약 10초 동안 작은 적을 여러 종류 또는 서로 다른 패턴으로 등장시킨다.
- 10초가 지나면 보스가 등장한다. 보스는 일반 적보다 크고 체력이 많으며, 최소 하나의 단순한 공격 패턴을 가진다.
- 적과 탄환에는 충돌 판정, 플레이어 체력 또는 목숨, 점수가 있어야 한다.
- 난이도는 초반에 조작을 익힐 시간을 주고, 시간이 지날수록 적의 수나 이동 패턴을 조금씩 늘리는 수준으로 조정한다.
- 보스를 처치하면 명확한 승리 상태를 보여준다. 플레이어가 쓰러지면 게임 오버 상태와 재시작 방법을 보여준다.
- 일시정지 같은 부가 기능은 핵심 게임 흐름을 해치지 않는 범위에서만 추가한다.

## 화면과 연출

- 배경은 최소한으로 유지하되, 별·선·원근감 있는 반복 요소가 오른쪽에서 왼쪽으로 흐르도록 하여 전진감을 분명히 한다.
- 플레이어, 적, 탄환, 보스는 색상과 크기가 확실히 달라서 한눈에 구분되어야 한다.
- HUD에는 점수, 체력 또는 목숨, 보스 등장까지의 진행 상태를 표시한다.
- 시작 화면에는 게임 제목과 시작 방법을 보여주고, 게임 오버와 승리 화면에는 재시작 버튼 또는 키를 제공한다.
- 데스크톱 키보드 플레이를 우선하되, 작은 화면에서도 HUD가 겹치거나 잘리지 않도록 반응형으로 만든다.
- 과한 파티클, 블룸, 복잡한 쉐이더, 무거운 애니메이션은 사용하지 않는다. 충돌과 피격은 짧은 색상 변화나 작은 효과로 표현한다.

## 코드 작성 원칙

- 가장 단순한 구현으로 요구사항을 만족한다. 추후 확장을 위한 과도한 추상화, ECS, 상태 관리 라이브러리는 도입하지 않는다.
- 숫자 밸런스와 게임 진행 시간은 한 곳에서 쉽게 수정할 수 있게 상수로 모은다.
- 게임 상태는 최소한 `start`, `playing`, `gameOver`, `victory`를 구분한다.
- 애니메이션은 `requestAnimationFrame`과 경과 시간(delta time)을 기준으로 처리해 프레임 속도에 따라 게임 속도가 달라지지 않게 한다.
- 콘솔 오류, 사용하지 않는 코드, 깨진 에셋 경로를 남기지 않는다.

## 완료 기준

- 새로 설치한 환경에서 의존성 설치 후 개발 서버로 실행된다.
- 시작, 이동, 발사, 적 처치, 보스 등장, 피격, 게임 오버, 승리, 재시작의 전체 흐름이 동작한다.
- 일반적인 데스크톱 화면과 좁은 화면에서 주요 UI가 겹치지 않는다.
- 구현 후 프로젝트의 실제 실행 명령과 테스트 또는 수동 검증 결과를 간단히 보고한다.

## 우선순위

1. 플레이어 조작과 게임 루프
2. 적, 충돌, 점수, 체력
3. 10초 후 보스와 승패 상태
4. HUD와 반응형 화면
5. 최소한의 시각 효과와 추가 기능

토큰과 유지보수 비용을 아끼기 위해 우선순위가 낮은 기능은 핵심 흐름이 완성된 뒤에만 추가한다.

